# SOP Drift Detector

MyClick Backend · Click.uz

Скрипт, который автоматически находит расхождение между задокументированными процессами (Confluence) и реальными изменениями в коде (Jira + GitLab). Раз в неделю анализирует закрытые задачи, смерженные MR и связанную документацию, и генерирует HTML-отчёт со страницами, которые нужно обновить.

## Проблема

Команда хранит документацию в Confluence (space `Products`), задачи ведёт в Jira (`MYCLICK`), код — в GitLab. После того как инженер закрывает задачу и мержит MR, связанные страницы Confluence часто остаются устаревшими — никто не обновляет документацию вручную. Со временем это приводит к тому, что:

- новые члены команды работают по устаревшим инструкциям;
- AI-агенты и автоматизации используют неактуальные данные;
- рассинхронизация между кодом и документацией накапливается месяцами.

## Как это работает

Скрипт последовательно проходит 5 этапов (`src/index.ts`):

1. **Jira** (`src/clients/jira.client.ts`) — получает задачи проекта `JIRA_PROJECT_KEY` со статусом `Test`, обновлённые за последние `DAYS_BACK` дней, и оставляет только те, чей заголовок начинается с `JIRA_TITLE_PREFIX` (по умолчанию `[Back]` — только backend-задачи):
   ```
   GET /rest/api/2/search?jql=project=MYCLICK AND status=Test AND updated>=-7d
   ```
2. **GitLab** (`src/clients/gitlab.client.ts`) — для каждой задачи ищет смерженный MR по ключу задачи и забирает diff изменённых файлов (бинарные и lock-файлы отфильтровываются, diff каждого файла обрезается до 10 000 символов):
   ```
   GET /api/v4/projects/{id}/merge_requests?search=MYCLICK-123&state=merged
   GET /api/v4/projects/{id}/merge_requests/{iid}/diffs
   ```
3. **LLM-анализ** (`src/analyzers/diff.analyzer.ts`) — diff отправляется в Claude (`claude-haiku-4-5-20251001`), который возвращает JSON: краткое описание изменения в поведении системы, ключевые слова для поиска в документации и флаг `hasDocImpact`. Если LLM недоступен или диффов нет — используется fallback на ключевые слова из заголовка задачи и MR.
4. **Confluence** (`src/clients/confluence.client.ts`) — рекурсивно (до 5 уровней вложенности) собирает все дочерние страницы под `CONFLUENCE_ROOT_PAGE_ID`, вместе с датой последнего обновления и текстом страницы (`body.storage`, очищенным от HTML).
5. **Матчинг и отчёт** (`src/analyzers/matcher.ts`, `src/reporters/html.reporter.ts`) — ключевые слова сопоставляются и с заголовком, и с содержимым страницы (заголовок даёт больший вес). Страницы со скором ниже порога релевантности (`MIN_SCORE = 3`) отбрасываются как шум. Страница считается устаревшей (`isDrifted`), если она не обновлялась дольше 14 дней или обновлялась раньше, чем был смержен MR. Топ-3 совпадения на задачу попадают в HTML-отчёт.

## Структура проекта

```
sop-drift-detector/
├── src/
│   ├── clients/
│   │   ├── jira.client.ts        # API-клиент Jira
│   │   ├── gitlab.client.ts      # API-клиент GitLab
│   │   └── confluence.client.ts  # API-клиент Confluence
│   ├── analyzers/
│   │   ├── diff.analyzer.ts      # LLM-анализ diff'а
│   │   └── matcher.ts            # матчинг изменений → страниц Confluence
│   ├── reporters/
│   │   └── html.reporter.ts      # генерация HTML-отчёта
│   ├── config.ts                 # конфигурация из .env
│   └── index.ts                  # точка входа
├── reports/                      # сгенерированные отчёты (не коммитятся)
├── tsconfig.json
├── .env.example
├── .env                          # секреты, не коммитится
├── package.json
└── README.md
```

## Требования

- Node.js 20+
- Yarn (в репозитории закоммичен `yarn.lock`)
- Доступ и токены к Jira, GitLab, Confluence и Anthropic API

## Установка

```bash
git clone <repo>
cd sop-drift-detector
yarn install
cp .env.example .env
# заполнить .env реальными токенами и URL
```

## Конфигурация

Все переменные обязательны, кроме отмеченных как опциональные (см. `src/config.ts`).

| Переменная                | Описание                                                                 |
| ------------------------- | ------------------------------------------------------------------------- |
| `JIRA_URL`                | Базовый URL Jira                                                          |
| `JIRA_TOKEN`              | Personal Access Token (отправляется как `Authorization: Bearer`)          |
| `JIRA_PROJECT_KEY`        | Ключ проекта (опционально, по умолчанию `MYCLICK`)                        |
| `JIRA_TITLE_PREFIX`       | Обрабатывать только задачи с этим префиксом в заголовке (опционально, по умолчанию `[Back]`) |
| `GITLAB_URL`              | Базовый URL GitLab                                                        |
| `GITLAB_TOKEN`            | Private Token с доступом к проекту                                        |
| `GITLAB_PROJECT_ID`       | ID проекта в GitLab                                                       |
| `CONFLUENCE_URL`          | Базовый URL Confluence                                                    |
| `CONFLUENCE_TOKEN`        | Personal Access Token (отправляется как `Authorization: Bearer`)          |
| `CONFLUENCE_ROOT_PAGE_ID` | ID корневой страницы, под которой ищутся дочерние                         |
| `ANTHROPIC_API_KEY`       | Ключ Anthropic API для LLM-анализа                                        |
| `DAYS_BACK`               | За сколько дней назад искать задачи (опционально, по умолчанию `7`)       |

## Запуск

```bash
# Сборка + разовый запуск
yarn dev

# Или по отдельности
yarn build      # компиляция TypeScript → dist/
yarn start      # запуск скомпилированного скрипта (node dist/index.js)
```

Скрипт создаёт файл `reports/report-YYYY-MM-DD.html`.

### Автоматический запуск (например, каждый понедельник в 9:00)

```cron
0 9 * * 1 cd /path/to/sop-drift-detector && yarn build && yarn start
```

## Формат отчёта

HTML-отчёт (`reports/report-YYYY-MM-DD.html`) содержит для каждой обработанной задачи:

- ключ и название задачи Jira;
- ссылку на MR в GitLab и дату мержа;
- LLM-описание того, что изменилось, и ключевые слова;
- список связанных страниц Confluence с датой последнего обновления;
- статус страницы: ⚠️ Устарела / ✅ Актуальна.

Пример записи:

```
MYCLICK-456 | Изменена логика авторизации через OTP
  MR: "feat: update otp validation flow" (merged 10 Sep 2026)
  LLM: "изменён алгоритм валидации OTP кода"
  ⚠️  "OTP авторизация" — обновлялась 3 месяца назад
  ⚠️  "Auth Flow" — обновлялась 5 месяцев назад
```

## Ограничения v1.0

- Скрипт только читает данные — не обновляет документацию автоматически.
- Матчинг основан на пересечении ключевых слов LLM с заголовком и текстом страницы — возможны как пропуски, так и ложные срабатывания на общих словах.
- Если MR не содержит ключ задачи (например, `MYCLICK-123`) в названии — связь между задачей и MR не находится.
- Обрабатывается только первый найденный MR на задачу; остальные MR той же задачи не анализируются.
- LLM анализирует только текстовый diff, бинарные и lock-файлы игнорируются.
- Работает с одним проектом Jira, одним проектом GitLab и одним деревом страниц Confluence за раз (без мультитенантности).

## Roadmap

**v1.0 — MVP (текущий)**

- Детекция устаревших страниц
- HTML-отчёт
- Ручной запуск / cron

**v1.1**

- Уведомления в Telegram с дайджестом
- Настройка через `config.json` без `.env`
- Поддержка нескольких проектов Jira

**v2.0**

- LLM генерирует черновик обновления страницы
- Confluence Draft API — создание черновика на review
- Web UI для просмотра отчётов
- Трекинг метрик: сколько страниц было обновлено после алерта

**SaaS (долгосрочно)**

- Мультитенантная архитектура
- Поддержка Notion, GitHub, Jira Cloud
- Интеграция с Slack / Teams / Telegram
- Dashboard с историей дрейфа

## Технический стек

- **Runtime**: Node.js 20 + TypeScript 5.9
- **HTTP-клиент**: axios
- **LLM**: Anthropic SDK (`@anthropic-ai/sdk`), модель `claude-haiku-4-5-20251001`
- **Отчёты**: нативный HTML (`fs.writeFileSync`)
- **Env**: dotenv
